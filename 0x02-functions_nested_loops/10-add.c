#include "main.h"
/**
  *add - adds two integers
  *@x: the first parameter
  *@y: the second parameter
  *Return: the sum of two parameters
  */
int add(int x, int y)
{
	int res;

	res = x + y;
	return (res);
}
