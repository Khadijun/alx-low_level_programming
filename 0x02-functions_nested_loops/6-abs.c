#include "main.h"
/**
  *_abs - displays absolute value of an integer
  *@x: parameter that specifies a number
  *Return: returns positive integers
  */
int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
